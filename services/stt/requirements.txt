# GPU STT. torch is installed separately by the host-matching CUDA build
# (inherited from the base image's venv). Two engines:
#   sensevoice (default) — FunASR SenseVoiceSmall: transcript + emotion + events
#   whisper   (fallback) — faster-whisper / CTranslate2
funasr>=1.1.0
faster-whisper>=1.0.3
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
numpy>=1.26
