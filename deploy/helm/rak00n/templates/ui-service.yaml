{{- if .Values.ui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: rak00n-ui
  namespace: {{ include "rak00n.namespace" . }}
  labels:
    {{- include "rak00n.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
spec:
  type: {{ .Values.ui.service.type | default "ClusterIP" }}
  selector:
    {{- include "rak00n.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ui
  ports:
    - name: http
      port: {{ .Values.ui.service.port | default 80 }}
      targetPort: http
      protocol: TCP
{{- end }}
